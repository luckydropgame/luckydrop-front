import { createStyles } from "@mantine/core";
export const useStyles = createStyles(() => {
  return {
    content: {
      width: "84vw",
    },
    title: {
      fontSize: "1.6rem",
      fontFamily: "inherit",
      lineHeight: 1,
    },
    value: {
      fontSize: "3.12rem",
      fontFamily: "inherit",
      lineHeight: 1,
    },
  };
});
